

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/css/bootstrap.css">
	<link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/usuarios.css">
  
    <% include partials/head %>

  

   
       <div class="caixa">
            <h2>Todos os Usuarios Cadastrados</h2>
            
            <table class="table table-border"> 
                <tr>
                    <th>Usuario</th>
                    <th>Data de nascimento</th>
                    <th>Telefone de casa</th>
                    <th>Movicel</th>
                    <th>Unitel</th>
                    <th>Provicia</th>
                    <th>Múnicio</th>
                    <th>bairro</th>
                    <th>Rua</th>
                    <th>genero</th>
                    <th>altura</th>

                    
                </tr>
            <% for(var i=0; i<usuarios.length; i++) {%>
                  <tr>
                <td><%= usuarios[i].usuario%></td>
                <td><%= usuarios[i].data_nascimento%></td>
                <td><%= usuarios[i].contacto.casa%></td>
                <td><%= usuarios[i].contacto.movicel%></td>
                <td><%= usuarios[i].contacto.unitel%></td>
                <td><%= usuarios[i].endereco.provincia%></td>
                <td><%= usuarios[i].endereco.municipio%></td>
                <td><%= usuarios[i].endereco.bairro%></td>
                <td><%= usuarios[i].endereco.rua%></td>
                <td><%= usuarios[i].genero%></th>
                <td><%= usuarios[i].altura%></th>
            </tr>


                   <% } %>

                </table>
            <div class="card-body">
            
      
            </div>
       </div>